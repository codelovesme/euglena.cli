import * as chai from "chai";

describe("node", () => {
    it("sample", () => {
        //given
        let target = true;
        //when
        //then
        chai.expect(target).to.be.true;
    });
});